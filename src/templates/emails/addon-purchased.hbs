
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Add-on Purchased</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #4CAF50; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9f9f9; }
        .footer { padding: 20px; text-align: center; color: #666; }
        .button { display: inline-block; padding: 10px 20px; background: #4CAF50; color: white; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Welcome to {{addOnName}}!</h1>
        </div>
        <div class="content">
            <p>Dear {{tenantName}},</p>
            <p>Congratulations! You've successfully purchased <strong>{{addOnName}}</strong>.</p>
            <p><strong>Amount Paid:</strong> {{amount}}</p>
            <p><strong>Transaction ID:</strong> {{transactionId}}</p>
            {{#if trialDays}}
            <p><strong>Trial Period:</strong> {{trialDays}} days</p>
            {{/if}}
            {{#if nextBillingDate}}
            <p><strong>Next Billing Date:</strong> {{nextBillingDate}}</p>
            {{/if}}
            <p>Your new features are now active and ready to use!</p>
            {{#if dashboardUrl}}
            <p><a href="{{dashboardUrl}}" class="button">Go to Dashboard</a></p>
            {{/if}}
        </div>
        <div class="footer">
            <p>LaundryPro Team</p>
        </div>
    </div>
</body>
</html>