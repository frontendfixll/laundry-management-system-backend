
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Trial Expiring Soon</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #2196F3; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9f9f9; }
        .footer { padding: 20px; text-align: center; color: #666; }
        .button { display: inline-block; padding: 10px 20px; background: #2196F3; color: white; text-decoration: none; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Trial Expiring Soon</h1>
        </div>
        <div class="content">
            <p>Dear {{tenantName}},</p>
            <p>Your trial for <strong>{{addOnName}}</strong> expires in {{daysLeft}} days.</p>
            <p><strong>Trial Ends:</strong> {{trialEndsAt}}</p>
            <p>Don't lose access to this powerful feature! Purchase now to continue using {{addOnName}}.</p>
            {{#if purchaseUrl}}
            <p><a href="{{purchaseUrl}}" class="button">Purchase Now</a></p>
            {{/if}}
        </div>
        <div class="footer">
            <p>LaundryPro Team</p>
        </div>
    </div>
</body>
</html>